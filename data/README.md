# Data setup

The assessment datasets are intentionally excluded from this public repository.

Place these four files in this directory before running the scripts:

- `train-test.csv`
- `validation.csv`
- `validation-predictions-template.csv`
- `december-chart-inputs.csv`

The scripts never overwrite these source files.
